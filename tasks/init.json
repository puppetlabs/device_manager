{
  "description": "Run puppet device on a (proxy) Puppet agent",
  "input_method": "stdin",
  "supports_noop": true,
  "parameters": {
    "target": {
      "description": "The name of a device in device.conf on the (proxy) Puppet agent",
      "type": "Optional[String[1]]"
    },
    "timeout": {
      "description": "The timeout in seconds for the puppet device command(s), defaults to 64",
      "type": "Optional[Variant[Integer, Pattern[/\A\d+\Z/]]]"
    }
  }
}
